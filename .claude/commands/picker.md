---
description: "Create a visual picker HTML to compare and choose between image/style/layout options"
---

Create a visual picker/comparison HTML page for the user to choose between options.

The user will specify what they're comparing (images, colors, styles, layouts, etc.). Create an HTML file at `C:/Users/mdehg/Dropbox/5_Courses/DADS5250-GenAI/planning/pick-[topic].html` that:

1. Uses the DADS 5250 theme (white background, Segoe UI font, brand blue #001a70, accent #0055d4)
2. Shows all options as clickable cards with labels and descriptions
3. Highlights selected option with green border (#059669)
4. Shows "Selected! Tell me the number and I'll apply it." message on click
5. For images: show full-width image previews
6. For text/colors/styles: show inline previews
7. Saves selection to localStorage

Then open the file in the browser with `start "" "path/to/file.html"`
